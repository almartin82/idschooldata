# Standardize column names across different file formats

Idaho SDE uses different column naming conventions across years and
files. This function maps them to a consistent set of names.

## Usage

``` r
standardize_column_names(df)
```

## Arguments

- df:

  Data frame with raw column names

## Value

Data frame with standardized column names
